import dotenv from 'dotenv';

dotenv.config();

export const {
  PORT,
  NODE_ENV,
  AUTH_SECRET,

  DB_SSL,
  DB_NAME,
  DB_USER,
  DB_PORT,
  DB_HOST,
  DB_PASSWORD,

  LOCATIONIQ_API_KEY,
  IP_GEOLOCATION_API_KEY,

  AWS_REGION,
  AWS_BUCKET_NAME,
  AWS_BUCKET_PATH,
  AWS_ACCESS_KEY_ID,
  AWS_SECRET_ACCESS_KEY,
  CREATE_CARD_LAMBDA_FUNCTION,
} = process.env;
